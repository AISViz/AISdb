#image:
#  name: alpine:latest

stages:
  - build
  - test
  - doctest

before_script:
  - mkdir -p ~/ais && export DATA_DIR=~/ais
  - apk add docker docker-compose sudo

create-package:
  type: build
  stage: build
  only: 
    - test-ci
  script:
    - sudo -E docker-compose build aisdb
