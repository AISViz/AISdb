#image:
#  name: alpine:latest

stages:
  - build
  - test
  - doctest

before_script:
  - apk add docker docker-compose
  - mkdir -p ~/ais
  - export DATA_DIR=~/ais

create-package:
  type: build
  stage: build
  only: 
    - test-ci
  script:
    - docker-compose build aisdb
