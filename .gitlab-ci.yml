#image:
#  name: alpine:latest

stages:
  - build
  - test
  - doctest

before_script:
  - uname -r
  - ls -lash
  - mkdir -p ~/ais && export DATA_DIR=~/ais

create-package:
  type: build
  stage: build
  only: 
    - test-ci
  script:
    - docker-compose build aisdb
