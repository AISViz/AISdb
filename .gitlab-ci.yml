#image:
#  name: docker/compose:1.29.2

#services:
#  - docker:dind

stages:
  - build
  - test
  - doctest

before_script:
  - mkdir -p ~/ais && export DATA_DIR=~/ais
#  - apk add --no-cache gcc py3-pip && pip install docker-compose

create-package:
  type: build
  stage: build
  only: 
    - test-ci
  script:
    - pip3 install .
